# O FROM É OBRIGATÓRIO E NESTE CASO ESTAMOS PEGANDO A ESTRUTURA DA IMAGEM QUE JÁ EXISTE NO DOCKER QUE É O openjdk
FROM openjdk
# O COMANDO WORKDIR CRIA UMA PASTA /app
WORKDIR /app
# ESTE COMANDO CRIA UMA VARIAVEL DE AMBIENTE
# ENV APP_EXECUTABLE=executable.jar 
ENV PASSWORD_SECRET="secret" 
# ESTE COMANDO COPIA PASTAS OU REALIZA DOWNLOADS DE URL
# CASO SEJA UTILIZADO UM ARQUIVO .zip PARA UMA PASTA, ELE VAI DESCOMPACTAR O ARQUIVO NO DIRETÓRIO INFORMADO
# NO COPY PASSAMOS "ARQUIVO" "DIRETÓRIO"
COPY src ./src
COPY pom.xml .
# EXPOR A PORTA USADA PELA APLICAÇÃO
EXPOSE 8080
